UI Requirements
Framework
React
TailwindCSS

Responsive rules
Mobile-first approach

Breakpoints
Mobile <= 640px
Tablet <= 1024px
Desktop >= 1024px

Layout
Mobile: single column
Tablet: two column grid
Desktop: three column grid

Implementation
Prefer CSS Grid
Avoid fixed widths
